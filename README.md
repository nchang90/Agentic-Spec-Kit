# Agentic Spec Kit

A collection of self-contained labs for building software and Azure
infrastructure with [Spec Kit](https://github.com/github/spec-kit), GitHub
Copilot, and executable engineering policy.

The central idea is simple:

1. Write standing rules in a constitution.
2. Specify observable outcomes without choosing an implementation.
3. Plan the technology and architecture.
4. Generate dependency-ordered tasks.
5. Implement, verify, and converge the result back against the specification.

This repository makes that loop concrete with focused demo and lab runbooks, an
Azure Verified Modules (AVM) infrastructure example, and a verifier
that turns the infrastructure constitution into executable checks.

## Start here

| Lab | Purpose | Azure cost |
|---|---|---:|
| [`01-legacy-vm`](lab/01-legacy-vm/) | Two presentation demos: durable specification followed by governed AVM infrastructure | None unless applied |
| [`02-weatherview`](lab/02-weatherview/) | Build and deploy an accessible static weather site | Free tier |
| [`03-aimarket`](lab/03-aimarket/) | Build a marketplace, semantic search, and grounded shopping assistant | About $0.13/hour while deployed |
| [`infra/`](infra/) | Terraform location reserved for the current legacy-VM demo | None until generated and applied |

Start with the two-part legacy-VM lab. The remaining labs are independent
application examples. See the [lab index](lab/) for the complete map.

## Repository map

```text
.github/
├── copilot-instructions.md    # Repository guidance for Copilot
├── scripts/                   # Acceptance and infrastructure verifiers
└── skills/                    # Spec Kit commands and reusable domain knowledge
.specify/
├── memory/constitution.md     # Current Azure platform policy
├── templates/                 # Artifacts generated by Spec Kit
└── workflows/                 # Spec Kit workflow definitions
lab/
├── 01-legacy-vm/              # Two-part Spec Kit and AVM presentation demo
├── 02-weatherview/            # Free Static Web Apps exercise
└── 03-aimarket/               # AIMarket runbook, assets, and AVM Terraform
infra/                         # Current legacy-VM demo Terraform
specs/                         # Example generated specification artifacts
```

The checked-in constitution is intentionally infrastructure-focused. Individual
application runbooks replace it on a scratch branch or in a separate workspace,
then restore it during cleanup.

## Prerequisites

All exercises require:

- VS Code
- GitHub Copilot Chat
- Git
- Node.js LTS
- the Spec Kit CLI

Azure deployment labs additionally require the tools listed in their prep
checklists. Do not deploy before reading the cost and teardown sections.

## Spec Kit commands

The current skills integration uses hyphenated command names:

```text
/speckit-constitution
/speckit-specify
/speckit-clarify
/speckit-plan
/speckit-checklist
/speckit-tasks
/speckit-analyze
/speckit-implement
/speckit-converge
```

## Verify the AVM reference

The verifier checks module sourcing and pinning, identity, observability,
reversibility, formatting, and Terraform validation:

```bash
terraform -chdir=lab/03-aimarket/infra init -backend=false
node .github/scripts/verify-avm.mjs --dir lab/03-aimarket/infra --offline
```

Omit `--offline` to verify each module's supported inputs against the Terraform
Registry. No Azure resources are created by either command.

## Safety

- Never commit credentials, `.tfvars`, Terraform state, local databases, or
  `.azure/`.
- Always run the documented validation gate before deploying.
- Always tear down lab resources the same day.
- Preview cleanup paths explicitly; do not use broad `git clean -fd` commands in
  a working repository.

## Repository status

This repository contains workshop source material and reference artifacts, not a
prebuilt AIMarket application. Application code is generated while following
the corresponding lab.
